{% extends "layout.twig" %}

{% block contenido %}
    <h1 class="title" id="listado">Productos con Stock Minimo</h1>
		<table summary="Tabla de productos del buffet">
		  <tr>
		    <th id="Nombre">NOMBRE</th>
		    <th id="Marca">MARCA</th>
		    <th id="Stock">STOCK</th>
		    <th id="Stock">STOCK MÍNIMO</th>
		  </tr>
			{% for producto in productos.iterator %}
				<tr id="{{ producto.id }}">
					<td>{{ producto.nombre }}</td>
					<td>{{ producto.marca }}</td>
					<td>{{ producto.stock }}</td>
					<td>{{ producto.stock_minimo }}</td>
				</tr>
			{% endfor %}
		  </table>
{% include 'paginacionFaltantes.twig' with {'pagesCount' : pagesCount} %}
{% endblock %}
